services:
  edge:
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./edge/conf.d:/etc/nginx/conf.d:ro
      - ./edge/certbot:/var/www/certbot:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
